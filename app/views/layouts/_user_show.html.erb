<p>User info</p>
 <div><%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg' %>

<p>name</p><%= user.name %>
<p>introduction<%= user.introduction %></p>

<%= link_to"Edit", edit_user_path(user.id) %>
</div>

<h1>New book</h1>

<%= form_for(newbook) do |f| %>
<h4>Title</h4>
<%= f.text_field :title %>
<h4>Opinion</h4>
<%= f.text_area :body %><br>
<%= f.submit 'Create Book', class: "btn btn-primary" %>
<% end %>
